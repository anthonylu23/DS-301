# PyTorch is cluster- and CUDA-specific on OOD. Prefer the preinstalled build
# or install it separately for your environment instead of forcing a pip upgrade here.
transformers>=4.19
wandb>=0.25
python-dotenv>=1.0
pandas>=1.5
matplotlib>=3.7
